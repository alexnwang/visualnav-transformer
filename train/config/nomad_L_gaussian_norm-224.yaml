project_name: nomad
run_name: nomad_L_gaussian_norm-224

# training setup
use_wandb: True # set to false if you don't want to log to wandb
train: True
batch_size: 32
epochs: 30
gpu_ids: [0]
num_workers: 12
lr: 1e-4
optimizer: adamw
clipping: True
max_norm: 1.
scheduler: "cosine"
warmup: True 
warmup_epochs: 1
cyclic_period: 10
plateau_patience: 3
plateau_factor: 0.5
seed: 0

# model params
model_type: nomad
vision_encoder: nomad_vint
encoding_size: 384
obs_encoder: efficientnet-b0
attn_unet: False
cond_predict_scale: False
mha_num_attention_heads: 8
mha_num_attention_layers: 8
mha_ff_dim_factor: 4
down_dims: [256, 384, 512]

# diffusion model params
input_dims: 48
num_diffusion_iters: 10

# mask 
goal_mask_prob: 0.5

# normalization for the action space
normalize: True

# context
context_size: 3 # 5
alpha: 1e-4

# distance bounds for distance and action and distance predictions 
distance:
  min_dist_cat: 0
  max_dist_cat: 20
action:
  min_dist_cat: 3
  max_dist_cat: 20

# action output params
len_traj_pred: 8


# dataset specific parameters
image_size: [224, 224] # width, height
datasets:
  nymeria:
    data_folder: /home/alexnwang/nymeria_dataset/nymeria_visibility_matrices_with_offsets_v2
    train: /home/alexnwang/wm_fbc-ar_model/data_splits/nymeria/train/
    test: /home/alexnwang/wm_fbc-ar_model/data_splits/nymeria/test/
    action_stats:
      min: [-2.5, -2.5, -2.5, -3.14159, -3.14159, -3.14159]
      max: [1.5, 1.5, 1.5, 3.14159, 3.14159, 3.14159]
    gaussian_normalization_stats_path: "/home/alexnwang/visualnav-transformer/train/logs/nomad_analysis/nomadanalysis/mean_var_stats.json"

# logging stuff
## =0 turns off
print_log_freq: 500 # in iterations
image_log_freq: 1000 #0 # in iterations
num_images_log: 8 #0 
pairwise_test_freq: 0 # in epochs
eval_fraction: 0.25
wandb_log_freq: 50 # in iterations
eval_freq: 1 # in epochs

eval_batch_size: 128